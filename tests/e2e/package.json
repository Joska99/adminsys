{
  "name": "adminsys-e2e",
  "private": true,
  "description": "Playwright UI/interaction tests. Isolated from the zero-dep app.",
  "scripts": {
    "test": "playwright test",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
